box : wercker/ubuntu12.04-nodejs0.8
sservices :
  - wercker/redis
build :
  steps :
    - sleep:
        name : sleepzortz
  stepss :
    - nodeenv activate
    - npm install
    - npm test
    - hello world
deploy:
  steps:
    - script:
        name : show
        code : |
          echo $WERCKER_SOURCE_DIR
          cd $WERCKER_SOURCE_DIR
          ls
          echo $WERCKER_OUTPUT_DIR
          cd $WERCKER_OUTPUT_DIR
          ls

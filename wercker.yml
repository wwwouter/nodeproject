box : wercker/nodejs
services :
  - wercker/redis
build :
  steps :
    - nodeenv activate
    - npm install
    - npm test
    - hello world
    - script :
        code : |
          echo 1
          echo 2
    - hello world
    - copy output:
        exclude : node_modules .idea .git .gitignore .gitmodules .jshintignore .jshintrc
